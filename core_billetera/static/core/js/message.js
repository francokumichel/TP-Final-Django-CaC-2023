const notificacionSwal=(titletext, text, icon) => {
    Swal.fire({
        titletext: titletext,
        text: text,
        icon: icon,
    });
};

(function() {

    notificacionSwal("Felicitaciones","Esto esta funcionando", error);


})